{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "onCreateCommand": "chmod +x scripts/setup/init.sh && scripts/setup/init.sh && chmod -x scripts/setup/init.sh && sleep 3",
  "postCreateCommand": "git config core.fileMode false",
  "postStartCommand": "bash -i -l -c 'exit 0'",
  "remoteEnv": {
    "DEVELOPER": "${localEnv:DEVELOPER}",
    "NO_REPLY_EMAIL": "${localEnv:NO_REPLY_EMAIL}",
    "SHELL": "/bin/bash"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.automationShell.linux": "/bin/bash"
      }
    }
  },
  "waitFor": "onCreateCommand",
  "runArgs": ["--env-file", ".devcontainer/.env"]
}
